name: Scrape flight prices

on:
  schedule:
    # UTC times chosen so runs land at ~06:00, ~14:00, ~22:00 Argentina time
    # (ART = UTC-3); actual delivery is a bit later due to the jitter sleep
    # below plus scraping time. Minute offset (:13, not :00) is deliberate --
    # GitHub's own docs warn that schedule triggers queue up (and can be
    # delayed or dropped entirely) at the top of the hour under load; on
    # 2026-07-05 the 09:00 UTC run never fired at all, no run record, no
    # queued job, nothing -- quota and incidents both ruled out.
    - cron: "13 9 * * *"
    - cron: "13 17 * * *"
    - cron: "13 1 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: scrape-flight-prices
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Random jitter before scraping
        if: github.event_name == 'schedule'
        run: |
          SLEEP_SECONDS=$(( RANDOM % 1200 ))
          echo "Sleeping ${SLEEP_SECONDS}s before starting"
          sleep "$SLEEP_SECONDS"

      - name: Run scraper
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          SERPAPI_KEY: ${{ secrets.SERPAPI_KEY }}
          PABLO_EMAIL: ${{ secrets.PABLO_EMAIL }}
          DAVID_EMAIL: ${{ secrets.DAVID_EMAIL }}
          IS_TEST_RUN: ${{ github.event_name == 'workflow_dispatch' }}
        run: node index.js

      - name: Commit results
        run: |
          git config user.name "vuelos-tracker-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/prices.csv data/resumen-pablo.csv data/resumen-david.csv
          git diff --cached --quiet && echo "Sin cambios" && exit 0
          git commit -m "Actualiza precios $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          git push
